# NFMRadTools
A console tools app for NFM car maker.

# Usage
Type help for list of commands.
Use '-' to skip optional parameters. (Example: car.groups.new - dsw will create a new group with random name and DragShotWheel poly group mode since we skip optional parameter Name with -.
For array inputs separate entries with ';'.
